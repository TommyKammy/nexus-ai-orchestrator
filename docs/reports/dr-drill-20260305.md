# DR Drill Report (2026-03-05)

Date: 2026-03-05
Issue: https://github.com/TommyKammy/nexus-ai-orchestrator/issues/42

## Scope

- Compose recovery drill: restart and readiness recovery for `n8n`.
- K8s recovery drill: pod loss recovery for `redis` deployment in `executor-system`.

## Compose Drill Evidence

Baseline timestamp (UTC): `2026-03-05T10:06:44Z`
Post-recovery timestamp (UTC): `2026-03-05T10:06:53Z`

### Baseline

- `n8n` readiness: `{"status":"ok"}`
- `opa` health: `{}`

`docker compose ps` excerpt:

| Service | Baseline status |
|---|---|
| `ai-n8n` | `Up 18 minutes` |
| `ai-opa` | `Up 4 hours (healthy)` |
| `ai-postgres` | `Up 4 hours` |
| `ai-redis` | `Up 4 hours` |

### Recovery action

```bash
docker compose stop n8n
docker compose start n8n
```

### Post-recovery

- `n8n` readiness: `{"status":"ok"}`
- `opa` health: `{}`

`docker compose ps` excerpt:

| Service | Post-recovery status |
|---|---|
| `ai-n8n` | `Up 5 seconds` (after restart) |
| `ai-opa` | `Up 4 hours (healthy)` |
| `ai-postgres` | `Up 4 hours` |
| `ai-redis` | `Up 4 hours` |

Result: compose recovery path validated for n8n service restart.

## K8s Drill Evidence

Baseline timestamp (UTC): `2026-03-05T10:10:51Z`
Post-recovery timestamp (UTC): `2026-03-05T10:10:54Z`

### Baseline

- Deployment `redis`: `1/1` available
- Pod: `redis-5747d78bb6-cbxc4` running

### Recovery action

```bash
kubectl -n executor-system delete pod redis-5747d78bb6-cbxc4 --wait=false
kubectl -n executor-system rollout status deployment/redis --timeout=180s
```

### Post-recovery

- Deployment `redis`: `1/1` available
- Replacement pod: `redis-5747d78bb6-2tjpn` running

Result: k8s workload self-healing path validated for redis deployment.

## Notes / Gaps

- This drill validated service-level recovery paths. Full cluster-level DR failover (secondary region/cluster cutover) remains a scheduled operational exercise.
- During drill window, some non-target workloads in `executor-system` were unhealthy (outside this drill scope).
